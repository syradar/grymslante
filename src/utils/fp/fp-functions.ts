export {};
// export const sum = (a: number, b: number) => a + b;

// export const map = <T, U>(f: (value: T, index: number, array: T[]) => U) => (
//   x: T[]
// ): U[] => x.map<U>(f);

// export const simpleFilter = <T>(f: (value: T) => value is T) => (x: T[]) =>
//   x.filter(f);
